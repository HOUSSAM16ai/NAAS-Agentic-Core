# 🧠 الهوية المعرفية المحيطة — الحالة الصادقة

> **الحالة تعيش هنا؛ القانون يعيش في**
> [`ambient_identity_constitution.md`](ambient_identity_constitution.md)
> \+ [`../docs/architecture/AMBIENT_COGNITIVE_IDENTITY.md`](../docs/architecture/AMBIENT_COGNITIVE_IDENTITY.md).
> تحرسهما `scripts/fitness/check_ambient_identity.py`: دليلٌ لا يوجد ⇒ CI أحمر ·
> خانةُ فجوةٍ فارغة ⇒ CI أحمر · وحدةٌ `SEAM`/`ABSENT` ولها كود ⇒ CI أحمر ·
> حذفُ صفٍّ بصمت ⇒ CI أحمر.
>
> **قرار السجلّ:** D-269. **آخر تحقّق:** 2026-08-19.

---

## 1. سُلَّم الحالة

نفس سُلَّم §6.6 — لا سُلَّم ثانٍ: `ACTIVE` · `PARTIAL` · `DORMANT` · `ZOMBIE` ·
`PLANNED` · `SEAM` · `ABSENT`.

## 2. وحدات الطبقة الخامسة

| # | الوحدة | دليل | الحالة | الفجوة الصادقة | شرط الترقية |
|---|--------|------|--------|----------------|--------------|
| 1 | التقاط المفتاح | `config/secret_catalog.json` | `ACTIVE` | سبعةُ أبوابٍ منطبقة وثلاثةٌ مُصرَّحٌ عدم انطباقها؛ والالتقاط يُثبِت **الارتباط** لا القبول | — مُرقّاة فعلاً (والقبول تُثبِته الوحدة 3) |
| 2 | عقد الطبقة | `shared/ambient_memory/contract.py` | `ACTIVE` | يصف التوفّر وحده؛ ⛔ ولا حقلَ فيه يمكن أن يحمل نصّ طالب — وهذا تصميمٌ لا نقص | — مُرقّاة فعلاً |
| 3 | المسبار الحيّ | `shared/ambient_memory/probe.py` | `ACTIVE` | يُثبت التوفّر والقبول؛ ⛔ ولا يُثبت أنّ الطبقة **تفيد** الطالب بشيء — لأنّها لا تفعل بعد | — مُرقّاة فعلاً بالبرهان الثلاثي |
| 4 | الإعلان في `/health` | `app/core/providers_health.py` | `ACTIVE` | يُعلَن عند الإقلاع مرّةً؛ **ولا إعادةَ فحصٍ دورية** — فحالةٌ تدهورت بعد الإقلاع تبقى معروضةً قديمة | مسبارٌ دوريٌّ بميزانية زمنٍ مُعلَنة، أو إعلانُ زمن الفحص في الواجهة |
| 5 | إثبات E2E حيّ | `scripts/e2e/honcho_live_probe.py` | `ACTIVE` | يعمل في `live-e2e` وهي `workflow_dispatch` **لا** في `required-ci` — فالانحدار يُكتشَف بقرارٍ لا بكلّ دفعة | إدراجُه في وظيفةٍ إلزامية يتطلّب سرّاً متاحاً لكلّ PR، وهو ما لا يصحّ لـforks |
| 6 | كتابة إشاراتٍ مجهولة | — | `SEAM` | ⛔ **صفر كود عمداً**. ليس نسياناً: إرسالُ أيّ شيءٍ عن قاصرٍ إلى طرفٍ ثالث قرارُ حمايةٍ لا قرارُ PR | **ثلاثتها معاً**: موافقةٌ صريحة من الوليّ · تحديث `DATA_POLICY.md` و`SAFEGUARDING.md` · بوّابةٌ تُثبت أنّ المُرسَل مُشتقٌّ ومجهول ولا يحمل `content` |
| 7 | قراءة النمذجة (`/chat`) | — | `PLANNED` | لا شيءَ لتُستدلّ عليه: بلا كتابةٍ سابقة يبقى نموذج المستخدم فارغاً — فالقيمة الفعلية اليوم **صفر**، ويُقال ذلك | ترقيةُ الوحدة 6 أوّلاً، ثمّ برهانٌ ثلاثي على القراءة |
| 8 | حكمُ الطبقة في دور الطالب | — | `ABSENT` | ⛔ **ليس نقصاً بل قانوناً** (L3): الحقيقة من المحرّكات الرمزية، والذاكرة التربوية من `TutorState`/BKT | ⛔ لا شرط ترقية — يتطلّب ADR يُبطِل L3، وهو ما لا يُقترَح |
| 9 | مقعد `deepseek-harness` | `docs/adr/ADR-010-deepseek-harness-adoption.md` | `SEAM` | ⛔ صفر كود. monorepo بـpnpm، وتوصيلُه الحيّ يخرق قفل D-187 | استكمال `M1→M4` + حاجةٌ **مقيسة** لا تخدمها حلقةُ §0.13 القائمة |

## 3. ما أُثبت حيّاً (2026-08-19)

قياسٌ مباشر على `api.honcho.dev` من هذه الجلسة:

| القياس | النتيجة |
|---|---|
| `POST /v3/workspaces/list` بالمفتاح | **200** · `total=1` · مساحة العمل `ETAALIM` (أُنشئت 2026-08-13) |
| `POST /v2/...` و`/v1/...` | **404** — الإصدار `v3` بيانٌ مقيس لا تخمين |
| مفتاحٌ خاطئ | **401** ⇒ `unauthorized` (عطلُ إعدادٍ صريح، لا انقطاعُ شبكة) |
| مضيفٌ لا يُحلّ | ⇒ `unreachable` بلا رفعِ استثناء |
| مفتاحٌ غائب / فارغ | ⇒ `not_configured` بلا أيّ نداءِ شبكة |
| بيانات طالبٍ أُرسلت | **صفر** — النداء الوحيد استعلامُ قراءةٍ لقائمة مساحات العمل |

## 4. ما لا يُدَّعى (حدّ المصداقية · D-227)

- ⛔ لا يُقال إنّ المنصّة «تتذكّر الطالب عبر Honcho» — لا يُكتب فيها شيء.
- ⛔ لا يُنسَب إليها أثرٌ تعليمي: لا رقم، ولا «+X%» (قفل Learning-Gain · D-263).
- ⛔ ولا يُقال إنّ الالتقاط «تكاملٌ مكتمل»: هو التقاطٌ وإثباتُ توفّرٍ، لا أكثر.

## 5. قاعدة التحديث

1. **بناء قدرة** ⇒ ترقية الصفّ بالبرهان الثلاثي في **نفس** الـPR + إدخال في
   `.memory/decisions.md`.
2. **إضافة/حذف صفّ** ⇒ قرارٌ مكتوب + تحديث العدّاد في `check_ambient_identity.py`.
   ⛔ لا حذف صامت.
3. **الفجوة تُكتب دائماً** — الفراغ يُقرأ نجاحاً.
