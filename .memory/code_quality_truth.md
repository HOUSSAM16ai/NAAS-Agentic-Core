# حقيقة أدوات جودة الكود — ماذا وجدت كلٌّ منهما، وكم من ذلك صحيح

> **الحالة، لا القانون.** القرار الهندسي في `.memory/decisions.md` D-235.
> كل رقمٍ هنا **مؤرَّخ بمصدره** — رقمٌ بلا تشغيلٍ يُنسَب إليه يتقادم ثمّ يكذب (D-188).
> آخر تحقّق: **2026-08-09** · Qodana على `238633a` (تكرّر على `3b975d8` و`62ce4c2`) ·
> CodeScene على `a814655` و`62ce4c2`.

## 1) الأداتان تقيسان شيئين مختلفين — والخلط بينهما هو أصل التوقّع الخاطئ

| | Qodana | CodeScene |
|---|--------|-----------|
| **ماذا يفعل** | تحليل ساكن يبحث عن **أخطاء** في الكود | يقيس **قابلية الصيانة** (Code Health) ويستقبل **التغطية** |
| **متى يحكم** | على المستودع كلّه في كل تشغيل | على **الكود المتغيّر في الـPR** (delta) |
| **مخرَجه** | قائمة نتائج بدرجات (Critical/High/Moderate) | بوّابات جودة تنجح أو تفشل + لوحة تغطية |
| **أين يُقرأ** | `qodana.cloud/projects/b890K` | `codescene.io/projects/83416` |
| **حارسه في المستودع** | `fail-threshold` في `.github/workflows/qodana_code_quality.yml` | تطبيق GitHub (خارج المستودع) + وظيفة `codescene-coverage` في `ci.yml` |

⛔ **لا يُتوقَّع أن تجدا الشيء نفسه.** Qodana لم يقل شيئاً عن تعقيد الدوالّ، وCodeScene
لم يقل شيئاً عن `dict[str, any]`. كلٌّ يرى ما صُمِّم ليراه.

## 2) ما وجده Qodana — 2,411 نتيجة (على `238633a`)

**⚠️ العدد ليس درجة جودة.** فُحصت النتائج **الحرجة الثلاث عشرة يدوياً**، فكانت
**5 حقيقية و8 كاذبة** — أي أنّ «Critical» في Qodana **ليس مرادفاً لـ«خطأ حقيقي»**.

### الحرجة (13) — حكمٌ لكلٍّ

| الموضع | النتيجة | الحكم |
|--------|---------|-------|
| `app/services/serving/infrastructure/mock_model_invoker.py` — 5 مواضع | `dict[str, any]` (الدالّة المدمجة بدل `typing.Any`) | ✅ **حقيقي · أُصلح** |
| `scripts/set_admin_password.py:2` | «Hardcoded password» | ❌ كاذب — docstring فيه `postgres://user:pass@host` كمثال |
| `live_db_restructure.py:2` | «Hardcoded password» | ❌ كاذب — docstring |
| `observability/grafana/dashboards/90-...-planning-agent.json:330` | «curl-auth-header» | ❌ كاذب — مثال curl داخل لوحة نصّية |
| `.ona/automations.yaml:766` · `:1089` | «curl-auth-header» · «Password in URL» | ❌ كاذب — سكربتا تحقّق |
| `app/middleware/config/middleware_settings.py` — 3 مواضع | «Parameters to generic types must be types» على `ConfigValue` | ❌ كاذب — `ConfigValue` اسم نوعٍ ضمني صحيح؛ Qodana لا يتعرّف عليه بلا `: TypeAlias` |

**دليلٌ مستقلّ على أنّ «Hardcoded passwords» كاذبة:** بوّابة `check_no_committed_secrets`
تمرّ بدَينٍ مُجمَّد **فارغ**، وهي تبحث عن أنماط الأسرار **الحقيقية** (`sk-or-v1-` ·
`tvly-` · JWT · كلمة سرّ فعلية في رابط قاعدة بيانات).

### العالية (320) وما فُحص منها

| العدد | النتيجة | الحالة |
|------|---------|--------|
| 106 | دالّة/صنف/وحدة مهجورة | لم تُفحَص — تنظيف مُجدٍ |
| 95 | هروب زائد في regex | لم تُفحَص — تنظيف آلي |
| 44 | استيراد غير مستعمل | لم تُفحَص — تنظيف آلي |
| **28** | استدعاء كائن غير قابل للاستدعاء | ⚠️ **لم تُفحَص فرداً فرداً** — بعضها ديكوريترات (كاذبة على الأرجح)، وبعضها في مسار الطالب: `app/infrastructure/clients/orchestrator/probability_ui.py:156` · `microservices/planning_agent/graph.py:64` |
| 7 | وسيط افتراضي قابل للتغيير | ❌ كاذب — نمط تجميد الإغلاق المقصود (`customer_chat.py:462`) |
| 6 | متغيّر محلّي غير مربوط | ❌ كاذب في `app/services/chat/local_graph.py:889,900` — الحارس `if root_span_ctx:` يضمن ربط `obs` (س827–834)، والمحلّل لا يربط بين المتغيّرَين |
| 6 | كود غير قابل للوصول | ⚠️ **لم تُفحَص** |
| **1** | جملة بلا أثر — `app/services/analytics/predictive_analyzer.py:174` | ✅ **حقيقي · أُصلح** — استيعاب يُحسَب ويُرمى؛ `low_risk` لا وجود له في المستودع كلّه |

### المتوسطة (2,078) — أسلوبية
`Method is not declared static` 658 · `Unclear exception clauses` 315 ·
`Unused local symbols` 265 · `Duplicated code fragment` 237 ·
`String conversion without dunder method` 236 · `Protected member access` 175.

## 3) ما وجده CodeScene

**صحّة الكود (delta):** على `457205b` **أفشل الـPR** ببندين في ملفّ البوّابة الجديد:

```
_check_token_guard         cyclomatic complexity 9, threshold 9
_check_third_party_write   5 arguments, max 4
```

أُصلحا بإعادة بناء (تعقيد **7** · وسائط **3**)، فتحوّل الفحص إلى **موافقة**:
«6 Quality Gates Passed» على `a814655`. ⚠️ ثلاثٌ من الستّ لم تُسمَّ في المخرَج، فلا
يُدَّعى معرفتها.

**التغطية:** `codescene-coverage` ترفع **691 ملفّاً** من `main` (`62ce4c2`)، وباسم الفرع
الصحيح بعد إصلاح HEAD المنفصل. ⚠️ لم تُرفَع بايتاً واحداً قبل 2026-08-09.

## 3.5) الأداة الثالثة والأهمّ: **بوّابات المستودع نفسه**

في هذا العمل بالذات، الأدوات الخارجية الثلاث **مرّت جميعاً** على إصلاحٍ خاطئ، والذي
أوقفه بوّابةٌ كتبها المستودع لنفسه:

| الحدث | النتيجة |
|------|---------|
| Qodana صنّف `dict[str, any]` **Critical** | ✅ كشفٌ صحيح |
| أُصلحت إلى `dict[str, Any]` | Qodana ✅ · CodeScene ✅ · CodeRabbit ✅ — **الثلاثة مرّوا** |
| `check_no_new_any` (بوّابة محلّية · D-192) | ❌ **رفضت**: «5× `Any` بينما المسموح 0 — صنِّف النوع بدل توسيعه» |
| أُصلحت إلى `dict[str, object]` | الجميع أخضر |

**الدرس:** الأداة الخارجية تعرف **اللغة**، ولا تعرف **قانون مستودعك**. حظرُ `Any` قرارٌ
محلّي (D-192)، ولا محلِّل تجاري يعرفه. ولذلك ⛔ **لا يُستبدَل حارسٌ محلّي بأداةٍ خارجية** —
تُضاف إليه.

**تذكيرٌ بصنفٍ لا تجده أيّ أداة ساكنة:** كوارث `.memory/issues.md` الكبرى (ISS-144 ·
ISS-148 · ISS-149) كان **الكشف فيها سليماً والعطب في الأسبقيّة** — ترتيبُ مراحل تُنهي
دور الطالب قبل التي تخدمه. هذه أخطاء **دلالة وترتيب**، تجدها عقود الترانسكريبت وبوّابات
`scripts/fitness/` وحدها.

## 4) الحدود المُعلَنة — لا تُكتَم (L11)

| الحدّ | الأثر |
|------|-------|
| **8 من 13 نتيجة حرجة كاذبة** (مقيسة) | «Critical» يستدعي فحصاً بشرياً لا إصلاحاً آلياً |
| المِسنَن **عددي** لا لكل نتيجة | إصلاحُ مشكلة وإدخالُ أخرى في نفس الـPR **يمرّ** |
| CodeScene يحلّل **`main` وحده** | تغطية فروع الـPR تصل ثمّ **تُهمَل** حتى تُضاف الفروع في إعداداته |
| التغطية `--cov=app` فقط | `microservices/` و`shared/` **غير مقيسة**؛ إن عرضها CodeScene **0٪** بدل «بلا بيانات» فالرقم كاذب ⇒ يُضيَّق النطاق أو يُطفَأ الفحص (D-197) |
| `actions/download-artifact@v6` على Node 20 | تحذير D-141 باقٍ — **لا إصدار أحدث اليوم** |
| رمز Qodana مُعرَّف بمشروع Cloud | إعادة إنشاء المشروع تُبطِل السرّ وتُنتج `403`؛ الـrunbook في خطوة `Explain the failure` |

## 5) المفتوح

| البند | الحالة |
|------|--------|
| 28 «استدعاء غير قابل للاستدعاء» · 6 «كود غير قابل للوصول» | **غير مفحوصة** — داخل المِسنَن، تُفحَص عند الحاجة |
| 106 مهجور · 95 هروب regex · 44 استيراد غير مستعمل | تنظيف آلي مؤجَّل |
| 2,078 نتيجة متوسطة | أسلوبية — لا خطّة |
| الإنذارات الثمانية الكاذبة | ⛔ **لا تُسكَت** بـ`suppress` ولا يُضاف `: TypeAlias` لإرضاء الأداة — الكود صحيح، وتغييره لإسكات محلِّلٍ مخطئ يخدم الرقم لا القارئ |
