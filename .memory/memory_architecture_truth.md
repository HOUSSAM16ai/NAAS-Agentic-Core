# 🧠 معمارية الذاكرة — الحالة الصادقة

> **الحالة تعيش هنا؛ القانون يعيش في**
> [`../docs/architecture/MEMORY_ARCHITECTURE_DOCTRINE.md`](../docs/architecture/MEMORY_ARCHITECTURE_DOCTRINE.md).
> تحرسهما `scripts/fitness/check_research_doctrines.py` +
> `scripts/fitness/check_no_system_text_as_user.py`.
>
> **قرار السجلّ:** D-229. **آخر تحقّق:** 2026-08-08 (فحصٌ حيّ على قاعدة الإنتاج
> `cogniforge-overmind`: 2393 رسالة طالب · 2250 ردّاً · 2026-01-07 → 2026-08-03).

---

## 1) سُلَّم الحالة

نفس سُلَّم §6.6 — لا سُلَّم ثانٍ: `ACTIVE` · `PARTIAL` · `PLANNED` · `DORMANT` · `ZOMBIE`
· `SEAM` · `ABSENT`.

## 2) الطبقات الأربع ومالكوها

| # | الطبقة | المالك الوحيد | دليل | الحالة | الفجوة الصادقة |
|---|--------|---------------|------|--------|----------------|
| 1 | عاملة (Working) | مُركِّب الدور | `app/api/routers/customer_chat.py` | `ACTIVE` | لا سياسة إخلاءٍ مُعلَنة: السياق يُقصّ بالطول لا بالقيمة التربوية |
| 2 | حلقية (Episodic) | جدول الرسائل، كاتبٌ واحد | `app/core/db_schema_config.py` | `PARTIAL` | ٢٦ انفجار كتابةٍ مزدوجة (٥٢ صفّاً) في يونيو 2026؛ انحسرت بعده ولا بوّابة تمنع عودتها |
| 3 | دلالية (Semantic) | `student_bkt_analytics` + حالة المعلّم | `app/services/analytics/tutor_state_service.py` | `ACTIVE` | مُلحَق-فقط ومُختبَر؛ ولا مراجعة دورية للتقادم داخل الصفوف نفسها |
| 4 | إجرائية (Procedural) | وحدات `shared/*` الحتمية | `shared/intent/registry.py` | `ACTIVE` | مصدرٌ واحد محروس بالتكافؤ؛ والدَّين المُجمَّد يتقلّص ولم يبلغ الصفر بعد |

## 3) التحدّيات الأربعة العابرة للمجالات

| # | التحدّي | الحامل / الفارض | دليل | الحالة | الفجوة الصادقة |
|---|---------|------------------|------|--------|----------------|
| 5 | تسميم الذاكرة (MINJA) | `check_no_system_text_as_user` | `scripts/fitness/check_no_system_text_as_user.py` | `ACTIVE` | يفرض بنداً واحداً (لا نصّ نظامٍ بدور الطالب)؛ ⛔ ولا يُدَّعى أنّ الذاكرة «محميّة من الحقن» |
| 6 | التقادم الصامت | قاعدة D-188 + ملفّ القفل | `.runtime/truth_table.lock.json` | `ACTIVE` | مفروضة على الوثائق وملفّ القفل؛ **لا تُفحَص صفوف الذاكرة نفسها** بحثاً عن تقادم |
| 7 | النسيان الكارثي (المقابل الطلابي) | `FSRS-5` + `ReviewSchedulerSkill` | `shared/scheduling/fsrs.py` | `PARTIAL` | الجدولة حتمية وتعمل؛ **ولا قناة تذكير** تُخرِج الموعد إلى الطالب — `SEAM` مُعلَنة (D-220/D-221) |
| 8 | الاتساق بين النسخ | مصدرٌ واحد + بوّابة تكافؤ | `scripts/fitness/check_intent_single_source.py` | `ACTIVE` | الاختيار مُعلَن (لا CRDT)؛ والتكافؤ محروسٌ في خمسة مواضع |

## 4) نقطة التفتيش

| # | البند | دليل | الحالة | الفجوة الصادقة |
|---|-------|------|--------|----------------|
| 9 | `checkpointer` عقدٌ لا تفصيل تشغيل | `scripts/fitness/check_stategraph_runtime_backbone.py` | `ACTIVE` | النوع مُثبَت ومُتحقَّق منه؛ ولا ميزانية زمنٍ مُعلَنة لكلفة الحفظ لكل دور |

## 5) ما قيس حيّاً (2026-08-08) — الأساس الذي يُقارَن به لاحقاً

| القياس | القيمة |
|---|---|
| نصّ نظامٍ مكتوبٌ بدور الطالب («[توجيه تربوي]») | **٢٨ صفّاً** — كلّها في نافذة يونيو 2026، صفرٌ بعدها |
| كتابةٌ مزدوجة (انفجارات/صفوف) | **٢٦ / ٥٢** — كلّها يونيو 2026 |
| تسريب علامات داخلية `⟦⟧` | **١٣** — كلّها يونيو 2026 |
| شظايا لاتينية في ردود عربية | **١٥ في أغسطس · ٥٧ في يوليو** — ⚠️ ما زال حيّاً (ISS-150) |

## 6) قاعدة التحديث

1. **بناء قدرة** ⇒ ترقية الصفّ بالبرهان الثلاثي في **نفس** الـPR + إدخال في `.memory/decisions.md`.
2. **إضافة/حذف صفّ** ⇒ قرارٌ مكتوب + تحديث العدّادات في `check_research_doctrines.py`. ⛔ لا حذف صامت.
3. **الفجوة تُكتب دائماً** — الفراغ يُقرأ نجاحاً.
