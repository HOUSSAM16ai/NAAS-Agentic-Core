# Extension Seams — كيف تُضاف التقنيات دون كود ميت (D-173)

> **قاعدة YAGNI الصادقة:** لا نُنزِل stub لتقنية غير مستخدمة. بدل ذلك، نُوثّق **المقعد**
> (seam) الذي تُركَّب فيه التقنية عند الحاجة الفعلية — نقطة توصيل حقيقية موجودة اليوم،
> مع عقد جاهز حيث ينطبق. هذا يحقّق «القدرة المُتحقَّقة على الإضافة» بلا ZOMBIE.
>
> كل مقعد أدناه: **الحالة الآن** · **المقعد (الملف الحقيقي)** · **شرط التبنّي** · **خطوة التركيب**.

---

## 1. مقعد الأحداث (Event Seam) — Kafka / أي وسيط رسائل

- **الحالة الآن:** العمود الفقري للأحداث هو **outbox relay قائم على قاعدة البيانات**
  (`microservices/orchestrator_service/main.py` + `src/api/routes.py` — `OUTBOX_RELAY_ENABLED`).
  الأحداث تُكتَب في جدول outbox داخل نفس معاملة الكتابة (تسليم مضمون once)، ثم يُصرّفها relay.
- **المقعد:** مُصرِّف الـ relay هو نقطة التبديل. استبداله بمنتج Kafka = تغيير **مستهلك الـ relay**
  فقط، دون لمس مُنتِجي الأحداث (يبقون يكتبون في الـ outbox).
- **العقد جاهز:** `docs/contracts/asyncapi/event-bus.yaml` + `events-api.yaml` (AsyncAPI) تصف
  الرسائل بالفعل — لا حاجة لاختراع مخطط عند التبنّي.
- **⚠️ تحديث 2026-08-01 (D-201 · ADR-007):** المقعد **شُغِل جزئياً بقرارٍ صريح من المالك** لا
  ببلوغ شرط الحمل. المُتبنَّى فعلاً هو **الانضباط**: `shared/messaging` (ظرف · سجلّ مواضيع مغلق ·
  «مرّة واحدة» · DLQ)، بسائقٍ في الذاكرة **حيّ افتراضاً** يمرّ بنفس حلقة الاستهلاك. سائق Kafka
  وRedpanda **مُثبَتان حياً منذ 2026-08-01** (D-204): وظيفة `event-stack-live` في CI تُقلع
  الوسيط على كل PR وتُثبت رحلة نشر/استهلاك حقيقية + تخطّي إعادة التسليم + وصول الرسالة
  المسمومة إلى DLQ (10 فحوص، 0 فشل). صندوق الصادر يبقى آلية الذرّية؛ الناقل فان-آوت.
- **شرط التبنّي الأصلي (متى Kafka فعلاً؟):** عند تجاوز معدّل الأحداث ما يتحمّله الـ relay القائم
  على الـ DB، أو عند حاجة مستهلكين متعددين مستقلين (fan-out حقيقي). هذا الشرط **لم يتحقّق بعد**،
  والتبنّي كان قراراً معلَناً بكلفته لا استجابةً لحِمل.
- **خطوة التركيب:** أضف producer إلى المُصرِّف يكتب رسالة AsyncAPI إلى topic؛ حوّل المستهلكين
  للاشتراك على Kafka. لا تغيير في منطق الأعمال (المُنتِجون يبقون على الـ outbox).

## 2. مقعد المتجهات (Vector Seam) — Vector DB / RAG fine-tuning

- **الحالة الآن:** **pgvector ACTIVE** — عمود `embedding` في `bac_exercise_questions` (Supabase؛
  `bac_exercises` metadata فقط لتوليد المرشّحين بـ SQL). الاسترجاع الحي عبر
  `app/services/capabilities/bac_db_retriever.py` (D-099) + fallback نصّي على `knowledge_base/`.
  التضمينات موجودة لعيّنة من التمارين؛ التوسّع = backfill (`scripts/backfill_exercise_embeddings.py`).
- **المقعد:** `bac_db_retriever` هو طبقة candidate-gen-then-rerank المستقلة عن مصدر المتجهات.
  استبدال pgvector بمتجر مخصّص (Qdrant/Weaviate/…) = تغيير مُولِّد المرشّحين فقط، مع بقاء
  إعادة الترتيب متعددة الإشارات كما هي.
- **شرط التبنّي:** عند تجاوز حجم بنك التمارين ما يخدمه pgvector بكفاءة (مليارات المتجهات + فلاتر
  معقّدة). حتى ذلك: pgvector كافٍ ومُفهرَس.
- **خطوة التركيب (RAG الحيّ):** شغّل `scripts/backfill_exercise_embeddings.py` (idempotent —
  `WHERE embedding IS NULL`) عبر جسر `scripts/db_bridge.py` (HTTPS:443) لتوليد التضمينات على الصفوف
  الجديدة؛ الاسترجاع يعمل تلقائياً. لا كود جديد للاسترجاع.

## 3. مقعد سجل النماذج (Model Registry Seam) — أي مزوّد LLM

- **الحالة الآن (D-174):** المصدر القانوني الوحيد للسلسلة هو `shared/ai_models/model_chain.py`
  (dep-free): PRIMARY + fallback chain من نماذج `:free` على OpenRouter، مع حُرّاس (تجاوز 404/429
  آلياً — D-167). العقلان (`app/core/ai_config.py` + `microservices/orchestrator_service/src/core/
  ai_config.py`) يُبقيان حرفياتهما المُثبَّتة أمنياً (دفاع عميق ISS-079)، وبوّابة AST
  `scripts/fitness/check_model_chain_parity.py` تُثبت أن العقلين == المصدر القانوني (تكافؤ محروس آلياً
  بدل مرآة D-013 اليدوية).
- **المقعد:** `shared/ai_models/model_chain.py` + `ActiveModels` في كل عقل. إضافة مزوّد جديد = مدخل في
  السلسلة القانونية (اسم النموذج + مفتاح البيئة). النماذج المحظورة (reasoning-only، D-067) موثّقة صراحةً.
- **شرط التبنّي:** بنشمارك حي قانوني (نفس system prompt + سؤال رياضي) قبل ترقية أي نموذج إلى PRIMARY.
- **خطوة التركيب:** حدّث السلسلة القانونية في `shared/ai_models/model_chain.py`، ثم اعكس الحرفيات
  المُثبَّتة في **العقلين**؛ بوّابة parity تفشل CI عند أي انحراف؛ شغّل البنشمارك الحي. لا تغيير في مسار الاستدعاء.

## 4. مقعد أعلام المهارات (Skill Flags Seam) — آلية تبنّي التقنيات المُتحقَّقة

- **الحالة الآن:** `app/services/skills/registry.py` — كل مهارة لها `status ∈ {ACTIVE, FLAGGED, PARTIAL}`.
  **FLAGGED** = القدرة موصولة لكنها خلف علم بيئة (تُفعَّل عند رفع العلم). مثالان حيّان:
  `RetrievalRerankSkill` (LlamaIndex + Reranker، `ENABLE_RETRIEVAL_RERANK_SKILL`) و
  `MCPToolSkill` (MCP، `ENABLE_MCP_TOOL_SKILL`) — قدرات كانت DORMANT، فُعِّلت كمهارات اختيارية (D-100).
- **D-174 (لا تُحذَف مقاعد RAG/rerank):** `app/drivers/llamaindex_driver.py` + `reranker_driver.py`
  هما backing الفعلي لـ `RetrievalRerankSkill` (استيراد كسول داخل الدوال + إعادة تصدير في
  `app/drivers/__init__.py`؛ مُسجَّلة في `registry.py` وتُخدَم عبر `/api/v1/skills`). تصنيف
  runtime «ZOMBIE» يعني «غير مُستدعى بعد runtime» لا «بلا مستهلك» — الرسم البياني للاستيراد **حيّ**.
  حذفها يكسر إقلاع المونوليث ويُلغي مقعد RAG/Reranker. كذلك `education_council.py` مُستورَد من
  `app/services/chat/agents/orchestrator.py` (مقعد المجلس، حي بالاستيراد). القاعدة: هذه **مقاعد**
  تُرقّى بالبرهان الثلاثي، لا كود ميت يُحذَف.
- **المقعد:** نمط FLAGGED **هو** آلية تبنّي التقنيات: LlamaIndex/DSPy/Reranker/MCP تُركَّب كمهارة
  بعقد Pydantic + مقاييس Prometheus + اختبارات + علم، ثم تُرقّى إلى ACTIVE بعد الإثبات الثلاثي
  (import + call chain + runtime evidence).
- **شرط التبنّي/الترقية:** ACTIVE يتطلّب البرهان الثلاثي الحي (§6.6). FLAGGED افتراضه OFF (صفر تغيير سلوكي).
- **خطوة التركيب:** أنشئ Skill جديداً في `app/services/skills/` (contract + metrics + tests)، سجّله في
  `registry.py` مع علم، ثم فعّله في البيئة والتحقق الحي قبل الترقية.

## 5. تقنيات ABSENT-by-YAGNI (لا كود، شرط تبنّي صريح)

| التقنية | الحالة | شرط التبنّي | المقعد عند التبنّي |
|---------|--------|-------------|--------------------|
| **Kafka** | **ACTIVE (D-204)** — الانضباط والوسيط مُثبَتان في CI | تبنٍّ صريح من المالك (2026-08-01)؛ شرط الحِمل لم يتحقّق بعدُ | §1 (مُصرِّف الـ relay) · ADR-007 |
| **Temporal** | **PARTIAL (D-204)** — الخطط ACTIVE، الخادم مُثبَت SERVING، **العامل DORMANT** | تبنٍّ صريح من المالك لثلاثة أعمال دائمة | ADR-008 · `shared/workflows` |
| **TLM** (Trustworthy LM) | ABSENT (غير مُثبَّت) | حاجة مُثبَتة لطبقة ثقة/توجيه نماذج بالكلفة/الجودة | §3 (سلسلة ai_config) |
| **Kagent** | **DELETED (D-173)** | — (كان ZOMBIE محظوراً أمنياً «Invalid token»، صفر مستهلك حي) | — |
| **متجر متجهات مخصّص** | ABSENT | حجم بنك تمارين يتجاوز pgvector | §2 (bac_db_retriever) |

> **درس Kagent (D-173 Stage 5):** حُذف بالكامل (`app/services/kagent/` + `kagent_driver.py` +
> multi-agent workflow + 3 عُقد + توصيلة DI) لأنه ZOMBIE — DI-registered singleton مستهلكه الوحيد
> رسمٌ ميت لا يستدعيه إلا سكربت تحقق يدوي. القاعدة: القدرة بلا مستهلك حي تُحذَف لا تُترك stub.

---

## 6. الرؤية الثورية (أهداف هذه الجلسة — D-173)

> مصدر الحقيقة الحيّ لخارطة الطريق: `.memory/roadmap.md` (ملخّص CLAUDE.md §0.6). هذا القسم يوثّق
> **الأهداف المعمارية** التي وجّهت D-173.

- **API-first 100%:** كل خدمة مصغرة (العدد **مُشتَقّ** من `docs/contracts/openapi/*-openapi.json` — لا يُكتب
  يدوياً هنا؛ كان `11/11` بينما الدستور يقول غيره، وهو الموضع الذي وُلد منه D-209)
  لها عقد OpenAPI ملتزَم يغطّي
  مساراتها الفعلية، مفروض ببوّابة تكافؤ دلالية (`scripts/fitness/check_openapi_parity.py`). العقود = الحدود الصريحة بين الخدمات.
- **منظومة وكيلة قابلة للاستبدال:** العقل الحتمي وحدة واحدة (`probability_tutor_brain.py`)، والـ port
  المستقل (`probability_tutor.py`) يخدم الخدمة المصغرة — تكافؤ محروس بـ20 عقداً (split-brain gate).
  الرسمان (12-node chat + 9-node missions) كلاهما حي، والانتقال التدريجي للخدمات المصغرة محكوم
  بـ`ORCHESTRATOR_PROB_TUTOR_ENABLED` (رافعة رجوع فوري، نمط D-025).
- **قتل التعقيد (SOLID/KISS/DRY/YAGNI):** God-files قُتلت (D-163→D-172)، والملفات الضخمة فُكِّكت
  عبر مانيفستات DRY (`TUTOR/BRAIN/API/DOCTRINE/CUSTOMER_CHAT/GRAPH_SOURCE_FILES`) — كل استخراج = سطر
  واحد، البوّابات تقرأ المصدر المُركَّب.
- **خارطة القدرات (متى/كيف نُضيف):** المقاعد أعلاه هي الإجابة الملموسة على «هل نستطيع إضافة
  Kafka/VectorDB/RAG/LlamaIndex/DSPy/Reranker/MCP؟» — نعم، عبر مقعد موجود، بشرط تبنّي صريح، وبلا كود ميت.
- **مقياس النجاح الوحيد (§0.6):** فجوة الوهم = الأداء المدعوم − القدرة غير المدعومة المؤجَّلة. نُحسّن
  على تقليصها — لا على مدة الجلسة/عدد الرسائل/الرضا اللحظي.

**قاعدة الإغلاق:** أي قدرة تُضاف تُثبَّت بالبرهان الثلاثي (import + call chain + runtime evidence)
قبل إعلانها ACTIVE؛ حتى ذلك فهي FLAGGED أو موثّقة كمقعد — لا ZOMBIE.

---

## 7. مقاعد اللغات (Polyglot Seams) — D-185

> **القرار الحاكم:** `docs/adr/ADR-006-polyglot-language-adoption.md`.
> **القاعدة في سطر واحد:** *الحدّ هو العقد لا اللغة* — بوّابات المشروع
> (`check_openapi_parity` · `check_ports_consistency` · `check_service_catalog_parity`)
> تفحص **عقد OpenAPI + `/health` + `/metrics` + منفذاً مُسجَّلاً**، ولا تفحص اللغة إطلاقاً.
> فأي خدمة بأي لغة تلتزم الأربعة تدخل النظام **بلا تعديل بوّابة واحدة**.

**البرهان العملي:** `notation-service` (:8011، D-185) سطحه كلّه عقد وبياناته جدول حتمي وصفر
اعتماد على المونوليث — فتنفيذ بديل بـRust/Go يلتزم عقده يستبدله بلا لمس العقل التربوي.

**شرط التبنّي الموحَّد (الثلاثة معاً، وإلا فلا):**
1. عبء عمل حقيقي **مقيس** يعجز عنه Python/TypeScript.
2. عقد مُلتزَم في `docs/contracts/openapi/` **قبل** أول سطر منطق.
3. برهان ثلاثي حيّ (import + call chain + runtime evidence) قبل ACTIVE — وإلا DORMANT بالاسم.

### 7.1 TypeScript — **المرحلة الأولى مُتبنّاة (ACTIVE): العقود أنواعاً مُتحقَّقة**

- **الحالة الآن:** الواجهة JavaScript؛ العقود الـ13 صارت **أنواعاً مُولَّدة ومُتحقَّقة في CI**،
  لكن **كود الواجهة لم يُحوَّل بعد** (بيان صريح، لا ادّعاء).
- **المقعد:** `frontend/tsconfig.check.json` — الاسم غير الافتراضي **مقصود**: `next build`
  يشترط `typescript` + `@types/*` كتبعيات حقيقية حالما يرى `tsconfig.json` أو `.ts` في `app/`،
  وذلك يُعدِّل `package-lock.json` (تحقّق حيّ: فشل «Please install @types/node»).
- **العقد جاهز:** `scripts/contracts/generate_frontend_types.py` → `frontend/types/api.d.ts`.
- **البوّابة:** خطوتان في `frontend-tests` — تطابق المُولَّد مع العقود (`--check`) +
  `tsc -p tsconfig.check.json` بـ`strict`/`skipLibCheck:false`. أثبتت قيمتها فوراً بكشف عطب
  `$ref` بلا بادئة خدمة في المولِّد.
- **شرط إكمال التبنّي:** `npm i -D typescript @types/node @types/react` + التزام القفل، ثم
  إعادة التسمية إلى `tsconfig.json` والتحويل ملفاً ملفاً.

### 7.2 التسع الباقيات — مقاعد موثَّقة، **صفر كود** حتى يتحقّق الشرط

| اللغة | الدور المرشَّح | المقعد الحقيقي اليوم | شرط التبنّي (إضافةً للثلاثة أعلاه) | خطوة التركيب |
|-------|----------------|----------------------|-------------------------------------|--------------|
| **Rust** | محرّكات تحليل/تحقّق حتمية، أمان نوعي، WebAssembly | خدمة مصغّرة بعقد — `notation_service` هو القالب المرجعي | مسار حتمي ساخن يُقاس > 50ms في Python ويُستدعى في كل دور | `microservices/<name>/` + `Dockerfile` + عقد + سطر في `microservice_catalog.json` |
| **Go** | بوّابات، workers، relay عالي التوازي | `api_gateway` + مُصرِّف الـoutbox (§1) | تشبّع I/O مُقاس على البوّابة أو الـrelay تحت حمل حقيقي | استبدال خدمة قائمة **خلف عقدها** لا بجانبها |
| **WebAssembly** | تشغيل محرّك المحاكاة داخل المتصفّح (Phase 6 — Simulation) | `SimulationEngine` المُخطَّط (`.memory/simulation_engine.md`) | حاجة «مليون تجربة» داخل الكانفس بلا رحلة شبكة | Rust→wasm يُستدعى من الواجهة، بلا خدمة جديدة |
| **Kotlin** | تطبيق موبايل أصلي للطلاب | لا مقعد اليوم (المنصّة ويب) | قرار منتَج بإطلاق تطبيق أصلي | مستودع/وحدة منفصلة تستهلك نفس عقود API |
| **Julia** | محاكاة عددية وبنشمارك علمي | `app/core/foundations` (حتمي، dep-free) | نموذج رياضي يعجز عنه `foundations` بدقّة أو سرعة | خدمة حساب بعقد على غرار `foundations-service` |
| **Elixir/BEAM** | WebSocket واسع النطاق، تحمّل الأعطال | طبقة WS الحالية (`customer_chat` + `server.js`) | آلاف الاتصالات المتزامنة تكسر بروسيس Python واحداً | استبدال طبقة النقل خلف نفس عقد الأحداث (`shared/chat_protocol`) |
| **Scala** | معالجة تدفّق/قواعد على JVM | مقعد Kafka (§1) — ABSENT | تبنّي Kafka أولاً (شرطه في §5) | مستهلك أحداث بعقد AsyncAPI |
| **C/C++** | مكتبات أصلية عند الضرورة القصوى | امتداد Python أصلي | ضرورة أداء مُثبَتة لا يحلّها Rust | امتداد داخل خدمة قائمة، لا خدمة جديدة |
| **Mojo** | تسريع مستقبلي لأحمال الذكاء | **مراقبة فقط** | نضج اللغة + نظام حزم مستقرّ | لا يُدخَل الآن (توصية صريحة) |

> **الحدّ الأحمر:** ممنوع إضافة خدمة «Hello World» بلغةٍ لإثبات القدرة على تعدّد اللغات.
> عشر سلاسل أدوات بلا مستهلك = عشر مصادر انحراف — وهو بالضبط ما تمنعه قاعدة Kagent (§5).

---

## 8. بوّابة الدفع (SATIM / CIB) — مقعد موثَّق، **صفر كود** (D-198)

**القرار:** المنصّة تُفتَح تجارياً بـ**القسائم المدفوعة مسبقاً** لا ببوّابة دفع.

**السبب ليس تقنياً:** شريحة كبيرة من أولياء الأمور في الجزائر لا تملك بطاقةً تعمل على
الإنترنت، فبوّابة الدفع تخدم أقلّيةً من السوق المستهدَف بينما القسيمة تخدمه كلّه — تُباع في
مكتبة أو عبر تحويل، وتُستبدَل في ثانية. وفوق ذلك يتطلّب تكامل SATIM تعاقداً وامتثالاً
واختباراتٍ لدى الطرف الثالث، وهي أعمالٌ لا تُنجَز داخل مستودعٍ برمجي.

| البند | الحالة |
|-------|--------|
| **المقعد الحقيقي اليوم** | `app/services/billing/` — `Entitlement` هو **العملة الوحيدة** التي يفهمها باقي النظام. القسيمة مجرّد **مصدر** لحقٍّ (`entitlements.source = 'voucher'`). |
| **شرط التبنّي** | عقدٌ موقَّع مع SATIM + بيئة اختبار + متطلّبات امتثال مكتوبة. |
| **خطوة التركيب** | مُصدِر ثانٍ للحقّ: `source = 'satim'` يكتب في نفس جدول `entitlements`. **لا يُلمَس** `require_active_entitlement` ولا أيّ معالِج — بوّابة الاشتراك تعريفٌ واحد بالفعل. |
| **ما يجب ألّا يحدث** | فحص اشتراكٍ منسوخ في المعالِجات. الموضع المنسيّ يصير ميزةً مجانية لا يعرف بها أحد؛ التعريف الواحد (`app/deps/billing.py`) هو ما يجعل إضافة مُصدِرٍ ثانٍ تغييراً في مكانٍ واحد. |

> **الحدّ الأحمر:** ممنوع كتابة عميل SATIM «جاهز للتفعيل» قبل توفّر العقد — قدرةٌ بلا
> مستهلك حيّ هي ZOMBIE بتعريف §6.6، ودفعُ الأموال أسوأ مكانٍ لكودٍ لم يُختبَر مقابل نظامٍ حقيقي.

---

## 9. مقعد Chargily Pay V2 — زميل §8، **صفر كود** (D-211)

**ما الجديد مقابل §8:** ‏SATIM تكاملٌ مباشر مع الشبكة النقدية، و**Chargily Pay V2** طبقةٌ
جزائرية فوقها تغطّي **EDAHABIA (بريد الجزائر)** و**CIB** معاً بواجهةٍ برمجية وحزمةٍ رسمية
وwebhooks. والفرق عمليّ: بطاقة EDAHABIA أوسع انتشاراً بين الأولياء من CIB، فالمقعد الذي
يخدم السوق فعلاً هو هذا.

هذا يعني أن الحاجز المذكور في دراسة السوق — «الاقتطاع الدوري غير منتشر في الجزائر» — له
**حلٌّ تقني منشور**، وأن الطبقة الغائبة قرارٌ لا عجز.

| البند | الحالة |
|-------|--------|
| **المقعد الحقيقي اليوم** | نفس مقعد §8: `Entitlement` هو العملة، والمُصدِر متغيّر. |
| **شرط التبنّي** | حساب تاجرٍ مُفعَّل + بيئة اختبار + **تحقّق توقيع webhook** مُختبَراً. |
| **خطوة التركيب** | `source = 'chargily'` يكتب في نفس جدول `entitlements`؛ و`Deposit` (العربون) كيانٌ من الدرجة الأولى بمرجع مزوّدٍ وولايةٍ وتاريخ؛ والدفتر **append-only**. **لا يُلمَس** `require_active_entitlement`. |
| **ما يجب ألّا يحدث** | عربونٌ يُسجَّل سطراً في جدولٍ يدوي بدل كيان — عندها يصير «الكشف البنكي» الموعود جدول Excel، وهو بالضبط نوع الدليل الذي يُرفَض. |

> **الحدّ الأحمر نفسه:** ممنوع عميل «جاهز للتفعيل» قبل العقد. والقانون الكامل في
> [`../REVENUE_ENGINE_SPEC.md`](../REVENUE_ENGINE_SPEC.md) (D-211)، والحالة في
> [`../../.memory/revenue_engine_truth.md`](../../.memory/revenue_engine_truth.md).
