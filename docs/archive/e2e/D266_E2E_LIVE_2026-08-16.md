# D-266 — البرهان الحيّ: «يجيب على كل سؤال» (2026-08-16)

> لقطةٌ مُجمَّدة. تُقرأ للتاريخ ولا تُحدَّث (قاعدة D-156/D-188).
> السكربت: `scripts/e2e/universal_answerability_live.py` · القرار: `.memory/decisions.md` D-266.

## البيئة — ما كان حيّاً فعلاً

| المكوّن | الحالة |
|---|---|
| قاعدة البيانات | **PostgreSQL 16 محلّية** (`postgresql+asyncpg://…/cogniforge`) — منفذا Supabase 6543/5432 محجوبان من هذه الحاوية، مُتحقَّق بـ`socket.create_connection` ⇒ `TimeoutError` |
| الخادم | المونوليث على `127.0.0.1:8000` · `/health` ⇒ `{"application":"ok","database":"ok"}` |
| **الأوركستريتور** | `127.0.0.1:8006` · `graph_ready=true` · `startup_state=ready` · `checkpointer_backend=**postgres**` · و`REQUIRE_ORCHESTRATOR=1` — أي أنّ **القلب الإلزامي للتوليد (D-112) هو من أجاب**، لا سقوطٌ محلّي |
| النماذج | **مفتاح OpenRouter حقيقي** · `LLM_MOCK_MODE=0` — سلسلة النماذج حيّة لا محاكاة |
| البروتوكول | WebSocket `/api/chat/ws` · المفتاح `question` · `subprotocols=['jwt', TOKEN]` (§6.6) |
| الحساب | طالبٌ مُسجَّل عبر `POST /api/security/register` ثمّ `login` (200) |

⚠️ **حدٌّ مُعلَن:** لم تُختبَر Supabase الإنتاجية من هنا — المنفذان محجوبان. وظيفة
`.github/workflows/live-e2e.yml` مُسلَّمة لتشغيل نفس المصفوفة على عدّاء GitHub مقابل
الإنتاج حين تُضبَط الأسرار. **الحدّ يُقال ولا يُخفى** (§0).

## النتيجة — 14/14 · صفر مخالفة

| # | السؤال | المادة | الزمن | حروف | الحكم |
|---|---|---|---|---|---|
| 1 | السلام عليكم | — | 0.11s | 71 | ✅ تحيةٌ حتمية بلا LLM |
| 2 | اعطني تمرين الاحتمالات 2024 | رياضيات | 1.90s | 1036 | ✅ استرجاعٌ مُفهرَس |
| 3 | لم أفهم | رياضيات | 9.84s | 77 | ✅ استيضاحٌ لا إعادة اشتقاق |
| 4 | كيف نحسب عدد الحالات الممكنة | رياضيات | 31.13s | 1874 | ✅ إجراءٌ لسؤالٍ إجرائي (D-207) |
| 5 | **اشرح لي قانون أوم** | فيزياء | 57.92s | 1919 | ✅ **«الجهد… الدوائر الكهربائية»** |
| 6 | **ما هو مبدأ أرخميدس** | فيزياء | 30.99s | 1379 | ✅ الطفو والاستقرار |
| 7 | **ما الفرق بين الجهد والتيار** | فيزياء | 128.54s | 1822 | ✅ فيزياء لا احتمالات |
| 8 | **ما معنى الرمز Ω في الفيزياء** | فيزياء | 51.96s | 3014 | ✅ **لا «فضاء العيّنة»** |
| 9 | **ما هو التناضح الخلوي** | علوم | 13.31s | 1521 | ✅ الغشاء والتركيز |
| 10 | اشرح لي تركيب البروتين | علوم | 56.51s | 3311 | ✅ |
| 11 | اشرح لي الدالة الأسية | رياضيات | 106.75s | 2663 | ✅ رياضياتٌ غير احتمالية |
| 12 | واش راهو الاشتقاق؟ | رياضيات (دارجة) | 34.61s | 2194 | ✅ |
| 13 | Explique-moi la loi d'Ohm | فيزياء (فرنسية) | 12.03s | 1277 | ✅ |
| 14 | كيفاش نحسب مساحة الدائرة | رياضيات (دارجة) | 20.02s | 1139 | ✅ |

```
أدوار: 14 · أجابت: 14 · مخالفات: 0
✅ كل سؤالٍ أُجيب بإطارٍ نهائيٍّ واحد، من مادته، بلا تسريبٍ ولا دورٍ صامت.
```

**العقود المفروضة على كل دور:** إطارٌ نهائي **واحد** (§6.5) · محتوى غير فارغ (ISS-145) ·
صفر مفردات احتمالات في سؤالٍ من مادةٍ أخرى (**ISS-159**) · صفر أرقام التمرين المرجعي
(D-113 · ISS-148) · صفر نثرٍ لاتيني في ردٍّ عربي (ISS-150) · صفر نصّ نظامٍ بدور الطالب
(D-229) · كل مكوّن مُولَّد ضمن `KNOWN_UI_COMPONENTS`.

## الحفظ — مُتحقَّق على قاعدةٍ حقيقية

```
customer_messages: 28 صفّاً
user      | كيفاش نحسب مساحة الدائرة
assistant | ## شرح قانون أوم …
user      | Explique-moi la loi d'Ohm
```

## أحمر قبل الإصلاح — الشاهد

قبل تعديل سطرٍ واحد من الإصلاح، عقدُ الترانسكريبت أعطى (على مراحل الدور الحقيقية):

```
[iss159_physics_not_probability turn 1] 'اشرح لي قانون أوم': ظهر الممنوع 'كرات'.
--- الجواب ---
لنبدأ من فهمك أنت — نسحب 3 كرات دفعة واحدة من كيس فيه: 4 حمراء، 2 بيضاء، 5 خضراء.
سؤال واحد قبل أي حساب: أيّ الألوان يمكن أن تعطينا 3 كرات من نفس اللون؟ ولماذا؟
```

طالبٌ سأل عن **قانون أوم** فتلقّى تمرين كراتٍ في كيس. هذا هو بلاغ ISS-159 حرفياً،
مُعاد تمثيله حتمياً — و«لا يُغلَق بلاغ كارثة بلا عقدٍ يُثبَت أحمر قبل الإصلاح»
(D-186 · القاعدة ٦).

## بواباتٌ خضراء مصاحبة

- `make gates` ⇒ **57/57** (كانت 48 قبل وصل البوّابات السبع الميتة).
- عقود الترانسكريبت ⇒ **9/9**.
- `ruff check .` + `ruff format --check .` ⇒ نظيفان.
- المجموعة الكاملة ⇒ **4501 نجحت**؛ والإخفاقات الباقية **مُثبَتة سابقةً** بتشغيلها على
  شجرةٍ غير معدَّلة (بيئة محلية — `main` أخضر على CI).


---

## الجولة الثانية — الحزمة الكاملة عبر الأوركستريتور (D-112)

الجولة الأولى أعلاه شغّلت مسار المونوليث المحلّي (`REQUIRE_ORCHESTRATOR=0`). أُعيدت
المصفوفة كاملةً مقابل **الحزمة الكاملة**: المونوليث ⇐ `orchestrator-service :8006`
(الرسم جاهز · نقطة التفتيش على Postgres) مع `REQUIRE_ORCHESTRATOR=1`:

```
أدوار: 14 · أجابت: 14 · فشلٌ منطوق: 0 · صمتٌ تام: 0 · مخالفات: 0
✅ كل سؤالٍ أُجيب بإطارٍ نهائيٍّ واحد، من مادته، بلا تسريبٍ ولا دورٍ صامت.
```

شواهد من الجولة (نصّ الطالب كما وصله):

| السؤال | مطلع الجواب |
|---|---|
| اشرح لي قانون أوم | «قانون أوم هو قاعدة بسيطة تساعدنا نعرف العلاقة بين الجهد…» |
| ما معنى الرمز Ω في الفيزياء | «الرمز **Ω** (أوميغا) يُستخدم في الفيزياء لتمثيل **وحدة المقاومة الكهربائية**، وهي **الأوم**…» |
| ما هو التناضح الخلوي | «حركة الماء عبر الغشاء الخلوي شبه النفاذ من منطقة ذات تركيز منخفض…» |
| ما هو مبدأ أرخميدس | «أي جسم يُغمر في سائل يتلقى قوة دفع صاعدة مساوية لوزن السائل…» |

**والرمز Ω هو الشاهد الأدقّ:** كان يُجاب بـ«فضاء العيّنة» (ISS-159 · الجذر الرابع)،
وصار يُجاب بـ**الأوم**.

## بلاغٌ كاذب أنتجه الحارس نفسه — وكيف صُحِّح

أوّل تشغيلٍ للحزمة الكاملة أعلن **١٢ «دوراً صامتاً»**. ولم يكن أيٌّ منها صامتاً:
الخادم كان يُرسل إطار `error` يحمل رسالةً عربية صريحة في `payload.message`
(«النظام يتطلب الخدمات الذكية المتقدمة وهي غير متاحة حالياً») — والحارس يقرأ
`payload.content` وحده.

والسبب الحقيقي للفشل كان **بيئياً بحتاً**: الأوركستريتور يقرأ
`ORCHESTRATOR_DATABASE_URL` (لا `DATABASE_URL`)، فسقط إلى مضيفٍ افتراضي بمستخدم
`postgres` ⇒ `PoolTimeout` بعد ٣٠ ثانية ⇒ 500 ⇒ رفضٌ صريح من `REQUIRE_ORCHESTRATOR=1`.

وهذا نفس صنف **ISS-145** الذي أُغلق **بالتفنيد**: فحصٌ على المعيار الخطأ. ومعيارٌ
خطأ في أداة تحقّقٍ أسوأ من غيابها — يُنتج بلاغاتٍ كاذبة تستهلك ثقةَ من يقرؤها.
صُحِّح الحارس ليقرأ `message` ويميّز ثلاث حالات لا اثنتين: **أجاب** · **فشلٌ منطوق**
· **صمتٌ تام** — والفشل المنطوق يبقى مخالفةً (لم يُجَب) لكنه لا يُسمّى صمتاً.
