{
  "name": "cogniforge-frontend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "node server.js",
    "dev:next": "next dev --hostname 0.0.0.0 --port 5000",
    "build": "next build",
    "start": "node server.js",
    "lint": "next lint",
    "//typecheck": "D-185: TypeScript is fetched on demand (npx) instead of added as a devDependency, so package-lock.json stays byte-stable and `npm ci` cannot drift. tsconfig sets types:[] and app/types/env.d.ts declares the little we need, so no @types package is required. CI runs this exact command.",
    "typecheck": "npx -y -p typescript@5.6.3 tsc --noEmit"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "http-proxy": "^1.18.1",
    "katex": "^0.16.27",
    "next": "16.1.5",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "react-markdown": "^10.1.0",
    "rehype-katex": "^7.0.1",
    "remark-math": "^6.0.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-next": "^14.2.3"
  }
}
