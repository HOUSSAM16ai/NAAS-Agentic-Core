"""NAAS Verification Layer — مسار المنتج (D-267).

⛔ **حدٌّ معماري:** هذه الحزمة **لا تستورد** من `app/**` ولا `microservices/**` ولا
`shared.curriculum` ولا `shared.notation`. القلبُ الذي يعرف مجالاً بعينه ميزةٌ متنكّرة
لا منتج (L1/L2). والمجال يدخل من `adapters/` **بياناتٍ لا استيراداً**.

⛔ **وقفل D-187 قائم:** لا مُنفِّذ صندوقٍ مقروناً بعميل نموذجٍ لغويّ في وحدةٍ واحدة —
تنفيذُ الرقع العدائية حمولةُ `M1→M4` لا التفافٌ عليها.
"""

from __future__ import annotations

__all__ = ["__version__"]

__version__ = "0.1.0"
